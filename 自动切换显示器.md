# CachyOS 升级后 Udev 触发 ddcutil 失效排查与修复指南

## 📝 问题背景

在 CachyOS (基于 Arch Linux) 等滚动发行版中，系统升级（特别是 `systemd` 升级）后，原本通过 `udev` 规则直接执行硬件控制脚本（如 `ddcutil`）可能会失效。

**失效原因：**

1. **沙箱限制加严**：新版 `systemd-udevd` 引入了严格的后台沙箱，禁止直接在 Udev 的 `RUN` 指令中访问 `/dev/i2c-*` 等硬件接口，导致命令被静默拦截。
2. **环境变量缺失**：`ddcutil` 在无头 (Headless) 环境下执行时，找不到 `$HOME` 变量，无法定位并读取 `dynamic sleep cache` 缓存文件，从而报错退出或执行极慢。

**标准架构（最佳实践）：**

`USB 拔插事件` ➜ `Udev 规则` ➜ `触发 Systemd Service` ➜ `执行 Bash 脚本` ➜ `ddcutil (指定 Bus)`

## 🛠️ 核心配置文件

### 1. 脚本文件：负责执行切换命令

**路径：** `/usr/local/bin/usb_dac_remove.sh`

Bash

```
#!/bin/bash
# 注意：务必使用 --bus=9 直接指定 I2C 总线，跳过全局扫描，实现“秒切”
/usr/bin/ddcutil --bus=9 setvcp 60 0x10
```

*(注：如果更换了主板或显卡接口，总线号可能会变。运行 `sudo ddcutil detect` 查看显示器对应的 `I2C bus` 数字进行更新。)*

### 2. Systemd 服务：突破沙箱限制并提供环境变量

**路径：** `/etc/systemd/system/usb-dac-remove.service`

Ini, TOML

```
[Unit]
Description=Switch Monitor on USB DAC Remove

[Service]
Type=oneshot
# 必须指定 HOME 变量，否则 ddcutil 会报 Unable to determine dynamic sleep cache 错误
Environment="HOME=/root"
ExecStart=/usr/local/bin/usb_dac_remove.sh
```

### 3. Udev 规则：监听硬件事件并触发服务

**路径：** `/etc/udev/rules.d/99-usb-dac.rules`

代码段

```
# 当匹配到 SMSL DAC 断开时，异步启动 Systemd 服务
SUBSYSTEM=="usb", ACTION=="remove", ENV{PRODUCT}=="152a/85dd/*", ENV{DEVTYPE}=="usb_device", RUN+="/usr/bin/systemctl start --no-block usb-dac-remove.service"
```

*(注：`--no-block` 非常重要，它让 udev 触发服务后立刻释放进程，防止阻塞内核 USB 队列。)*

## 🚀 应用并生效

修改完以上文件后，依次执行以下命令使其生效：

Bash

```
# 1. 预热 ddcutil 缓存 (重要！生成 /root/.cache/ddcutil 加快后续执行速度)
sudo ddcutil detect

# 2. 重载 Systemd 与 Udev 配置
sudo systemctl daemon-reload
sudo udevadm control --reload-rules
```

## 🔍 未来排查指南 (Troubleshooting)

如果日后再次失效，请严格按照以下 **“从上到下”** 的顺序进行二分排查：

### 第一步：测试最底层的 ddcutil 和脚本

手动执行脚本，看显示器是否切换：

Bash

```
sudo /usr/local/bin/usb_dac_remove.sh
```

- **如果不切**：说明 `i2c-dev` 模块未加载，或 `I2C bus` 编号发生了变化（运行 `sudo ddcutil detect` 确认）。
- **如果正常切换**：进入第二步。

### 第二步：测试 Systemd 服务层

手动触发 Systemd 服务：

Bash

```
sudo systemctl start usb-dac-remove.service
```

- **如果失败**：查看报错日志：

    Bash

    ```
    journalctl -xeu usb-dac-remove.service
    ```

    *(常见报错是 HOME 环境变量丢失导致缓存写入失败，或权限不足)。*

- **如果正常切换**：说明服务正常，问题出在最前端的 Udev，进入第三步。

### 第三步：测试 Udev 监听层

查看 USB 设备拔插时的真实 ID 和环境变量：

Bash

```
udevadm monitor --property --subsystem-match=usb
```

拔插设备，观察弹出的日志，检查下面三个变量是否发生变化：

- `ACTION`（是 remove 还是 unbind？）

- `PRODUCT`（设备 ID 是否还是 `152a/85dd/xxx` 格式？）

    如果发生了变化，同步修改 `/etc/udev/rules.d/99-usb-dac.rules` 中的匹配条件即可。